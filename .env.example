# Base URL of your locally running Ollama server
OLLAMA_BASE_URL=http://localhost:11434

# Which Ollama model to use for generating edge cases
OLLAMA_MODEL=qwen3:4b

# AI mode: "demo" (no Ollama needed, uses canned data) or "ollama" (real local AI)
AI_MODE=demo
